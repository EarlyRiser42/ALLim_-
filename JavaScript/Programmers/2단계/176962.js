function htom(time){
    return Number(time.slice(3,5))+Number(time.slice(0,2))*60
}

function solution(plans) {
    var answer = [];
    let memory = []
    plans.sort(a=>a[1])
    while(plans){
        let cash = plans.shift()


    }
    return answer;
}